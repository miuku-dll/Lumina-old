<Page
    x:Class="Lumina.Views.Pages.FeaturesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Lumina.Views.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="clr-namespace:Lumina.Models"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="DataPage"
    d:DataContext="{d:DesignInstance local:FeaturesPage,
                                     IsDesignTimeCreatable=False}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    ScrollViewer.CanContentScroll="False"
    mc:Ignorable="d">

    <Grid VerticalAlignment="Top">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <StackPanel Width="600" Height="500">
            <ui:CardExpander Grid.Column="0" 
                            Margin="0,10,0,0" Icon="{ui:SymbolIcon Play24}">
                <ui:CardExpander.Header>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <ui:TextBlock
                Grid.Row="0"
                Grid.Column="0"
                FontSize="16"
                FontTypography="Body"
                Text="Auto Collect" />
                        <ui:TextBlock
                Grid.Row="1"
                Grid.Column="0"
                FontSize="12"
                Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"
                Text="Auto Collect Features"/>
                        <ui:ToggleSwitch
                Grid.Row="0"
                Grid.RowSpan="2"
                Grid.Column="1"
                Margin="0,0,16,0"
                OffContent="Off"
                OnContent="On" Checked="AutoCollectGlobal_Checked" Unchecked="AutoCollectGlobal_UnChecked" />
                    </Grid>
                </ui:CardExpander.Header>
                <StackPanel Margin="24,0.5,24,0">
                    <ui:CardControl Padding="20,10,20,10" Header="Auto collect all spots">
                        <ui:ToggleSwitch
                            HorizontalContentAlignment="Left"
                            IsEnabled="True"
                            OffContent="Off"
                            OnContent="On" Checked="ToggleSwitch_Checked"
                            Unchecked="ToggleSwitch_UnChecked"/>
                    </ui:CardControl>


                </StackPanel>
            </ui:CardExpander>
            

        </StackPanel>
    </Grid>
</Page>

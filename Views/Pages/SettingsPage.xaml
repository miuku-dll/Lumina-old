<Page
    x:Class="Lumina.Views.Pages.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:helpers="clr-namespace:Lumina.Helpers"
    xmlns:local="clr-namespace:Lumina.Views.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="SettingsPage"
    d:DataContext="{d:DesignInstance local:SettingsPage,
                                     IsDesignTimeCreatable=False}"
    d:DesignHeight="800"
    d:DesignWidth="800"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    mc:Ignorable="d">
    <Page.Resources>
        <helpers:EnumToBooleanConverter x:Key="EnumToBooleanConverter" />
    </Page.Resources>


    <Grid VerticalAlignment="Top">
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        
        <TextBlock
            FontSize="20"
            FontWeight="Medium"
            Text="Macro Settings" Grid.ColumnSpan="2" Margin="0,0,634,376" />

        <ui:Button
            Content="Save Hook!"
            x:Name="SaveWebhook"
            Icon="Fluent24" Height="36" Click="Button_Click" Margin="508,74,0,0" VerticalAlignment="Top" />
        <ui:TextBox
            x:Name="Webhook" 
            PlaceholderText="Enter webhook..."
            Width="500" Height="36" Margin="0,74,300,308" Grid.ColumnSpan="2"/>


        <TextBlock
            FontSize="20"
            FontWeight="Medium"
            Text="Personalization" Margin="0,238,0,-744" Grid.ColumnSpan="2" />
        <TextBlock Margin="0,272,566,16" Text="Theme" Grid.ColumnSpan="2" />
        <RadioButton
            Margin="0,335,0,0"
            Command="{Binding ViewModel.ChangeThemeCommand, Mode=OneWay}"
            CommandParameter="theme_light"
            Content="Light"
            GroupName="themeSelect"
            IsChecked="{Binding ViewModel.CurrentTheme, ConverterParameter=Light, Converter={StaticResource EnumToBooleanConverter}, Mode=OneWay}" Grid.ColumnSpan="2" VerticalAlignment="Top" />
        <RadioButton
            Margin="0,298,0,0"
            Command="{Binding ViewModel.ChangeThemeCommand, Mode=OneWay}"
            CommandParameter="theme_dark"
            Content="Dark"
            GroupName="themeSelect"
            IsChecked="{Binding ViewModel.CurrentTheme, ConverterParameter=Dark, Converter={StaticResource EnumToBooleanConverter}, Mode=OneWay}" VerticalAlignment="Top" Grid.ColumnSpan="2" />

        <TextBlock
            Margin="1,407,-1,-383"
            FontSize="20"
            FontWeight="Medium"
            Text="About Lumina" Grid.ColumnSpan="2" />

        <TextBlock
            FontSize="15"
            FontWeight="Regular"
            Foreground="#abadaf"
            Text="Webhook" Grid.ColumnSpan="2" Margin="1,45,633,331" />
    </Grid>
</Page>
